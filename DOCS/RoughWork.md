Limitation of Loading of all files which causes slownes and issues if any file contains error
Limitation of Writing in template instead logics should be written on command output and application should have a way to process them
Dyanmic Logic which can be automatically be derived by selecting areas from Log output in frontend and some basic actions like where to write in excel or output sheet prepration.
Pause for verification, Wait, CLI Execution through txt files should be present in excel
Command Mapping can be made easy instead of hardcoding rcode. Work with drag and drop types flow
Tester which help to tell command and how the automation performs without logging 
CLI Generator. Generates CLI for the provided input sheet with necessary command logs.
Think about Resusability.
Expect String Replacement
Retry Logic and Pooling Behaviour
Think About Design Automation Prepration. Can this much of data and conditions can be handled in Desgin Automations
Input Handler Folder Specific Automated Folder Creation Logic Deployment instead of manual moving
Make the normal files configurable with yes or no like connectivity Sheet, Execption Sheet and Final Ouput Sheet
Make sure nothing stays on server and evrything is added in DB and Final Sheet is just at last nothing should be stored
As the connectivity of real time node is not with the local so we cannot make ssh connection from local and test the node behaviour and this cannot be solved due to security issue but need to think of a solution which can make 90 to 95 percent confirmation of the development. This can be done by breaking module in smaller parts and asking node prompt output success and failure case
AUTOMATION trigger flow should be scallable which can help to scale application and support features like If Input provided from Teams, Outlook or any other event based trigger. It should not dependant on frontend entirely.
All add a automation execution timing if any automation is supposed to be executed in night only so it should not login the node in other window
Login Process can be selected and dont need to code again and again like M2M connection(Direct SSH), OSS Login, NIAM Login(Login though any IAM),openssl and etc in telecom
Option to provide manual CRID by user
User can select the sequnce of node if he needs that nodes should be processed in a specific rank manner
Batch creation can be in different methods like node wise, server wise, Combination of Near and Far end nodes
